{
  "name": "digicode-pixi-shapes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0", 
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
